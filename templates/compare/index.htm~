<html>

    <head>
        <title>Compare Mobiles</title>
    </head>
    
    <body>
        <h1>Find your perfect phone</h1>
        <h2>Select mobile platform</h2>
        
        {% if categories %}
            <ul>
                {% for c in categories %}
                    <li><a href = "{{ mobile.company }}">{{ c.platform }}</a></li>
                {% endfor %}
            </ul>
        {% else %}
            <strong>There are no categories present.</strong>
        {% endif %}
                
    </body>

</html>
